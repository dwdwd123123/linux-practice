## 挂载 mount（Rocky9 · 初学者版）
一句话理解：Linux没有C/D盘符。挂载=把磁盘绑定到一个文件夹（挂载点），访问文件夹就等于访问磁盘。

### 基础命令
mount + 设备路径 挂载点            # 临时挂载，重启失效
umount + 挂载点                   # 卸载，断开磁盘和目录绑定

### 查看命令
lsblk：看看电脑上有几块硬盘
df -h：看看已经绑定好的硬盘，还剩多少空间