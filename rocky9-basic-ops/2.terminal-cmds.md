### 终端命令

## 1.基础篇
ls                                           列出目录里面的文件和文件夹(list)

su                                           切换用户(swich user)
su root                                      切换到 root 用户，但不加载 root 完整环境变量
su - root/su - (后者默认切 root)              "-" 代表完全切换用户(切换身份 + 加载该用户全部环境变量、进入用户家目录)
su - "用户名"                                 切换至指定普通用户
